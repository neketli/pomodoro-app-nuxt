import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pomodoroappnuxt.capapp',
  appName: 'pomodoro-app-nuxt',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
